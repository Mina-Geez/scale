
from setuptools import setup, find_packages

setup(
    name="scale_integration",
    version="0.0.1",
    description="Scale Integration for ERPNext",
    author="Mina George",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
    zip_safe=False
)
